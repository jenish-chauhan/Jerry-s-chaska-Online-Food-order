apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  namespace: food-ordering
spec:
  type: NodePort
  selector:
    app: admin-panel
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
